export type BreadcrumbItemType = {
  href?: string;
  label: string;
};
export type ComboboxItemType = {
  value: string;
  label: string;
  currencyCode?: string;
};
export type ProfileInfo = {
  id: string;
  name: string;
  imgId: string;
}
